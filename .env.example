# ── Telegram (required) ──────────────────────────────────────
TELEGRAM_BOT_TOKEN=your-bot-token-here
# Chat ID for admin alerts (monitor, watchdog, errors)
ADMIN_CHAT_ID=your-chat-id-here

# ── Access control (optional) ───────────────────────────────
# Comma-separated Telegram user IDs. Empty = allow all.
ALLOWED_USER_IDS=

# ── Proxy (optional) ────────────────────────────────────────
# Single or comma-separated for rotation.
# Supports http, https, socks5.
# PROXY_LIST=socks5://user:pass@proxy1:1080,http://proxy2:8080
PROXY_LIST=

# ── Webhook notifications (optional) ────────────────────────
# WEBHOOK_URL=https://hooks.slack.com/services/xxx
# WEBHOOK_FORMAT=slack  # or: discord, (empty for raw JSON)
WEBHOOK_URL=
WEBHOOK_FORMAT=

# ── Monitor ─────────────────────────────────────────────────
# Page structure check interval in seconds (default: 3600)
MONITOR_INTERVAL=3600

# ── Logging ─────────────────────────────────────────────────
LOG_DIR=/app/logs

# ── Watchdog (set automatically by docker-compose) ──────────
# WATCHED_CONTAINER=tempmail-bot
# HEALTH_CHECK_URL=http://tempmail-bot:8080/health
# HEALTH_CHECK_INTERVAL=30
# HEALTH_CHECK_THRESHOLD=3
# HOST_METRICS_INTERVAL=60
# CPU_THRESHOLD=90
# RAM_THRESHOLD=85
# DISK_THRESHOLD=90
